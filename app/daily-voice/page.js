import VoiceDailyForm from "../components/VoiceDailyForm";

export const metadata = { title: "Daily report — voice-first" };

// Full-width host for the voice-first daily report (real phone use).
export default function DailyVoice() {
  return <VoiceDailyForm />;
}
